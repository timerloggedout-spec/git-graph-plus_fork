interface:
  display_name: "Git Graph Plus Fork"
  short_description: "Repo-specific patterns and workflows for git-graph-plus_fork"
  default_prompt: "Use the git-graph-plus_fork repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true